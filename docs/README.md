# React Workshop

## Getting started

1. Run `yarn create vite . --template react-ts`
   1. Choose to delete all files (if asked).
   2. Decline using roll-up (if asked).
2. Navigate to the URL presented in the terminal.
3. Start coding!

